caroline {
    mongoUrl = "mongodb://localhost"
    mongoUrl = ${?MONGO_URL}
    databaseName = "caroline"
    jwtIssuer = "localhost"
    jwtRealm = "Caroline"
    jwtSecret = "CHANGEMETOASECRET"
}

ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [
            drewcarlson.caroline.BundledServer.module,
            drewcarlson.caroline.CoreModule.coreModule,
            drewcarlson.caroline.ProjectModule.projectModule,
            drewcarlson.caroline.UserModule.userModule,
            drewcarlson.caroline.LoggingModule.loggingModule,
            drewcarlson.caroline.CrashModule.crashModule,
        ]
    }
}
